# Backend port for Vite dev server proxy (optional if using VITE_API_URL)
BACKEND_PORT=3001

# Full backend URL — set this so API requests go directly to the backend (no proxy).
# Local: http://localhost:3001  |  Remote: http://116.202.210.102:3001
VITE_API_URL=http://localhost:3001
